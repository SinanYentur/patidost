package com.patidost.app.core.ui.theme

import androidx.compose.ui.graphics.Color

// Main Palette from UI mockups
val DeepPurple = Color(0xFF483D8B)       // Dark Slate Blue from login screen gradient
val LightPurple = Color(0xFFC8A2C8)      // Lilac from login screen gradient
val GoldAccent = Color(0xFFFFD700)        // Gold color for highlights, paw icon
val SoftGold = Color(0xFFF0E68C)          // Khaki, a softer gold for backgrounds/cards

// Neutral & Text Colors
val TextWhite = Color(0xFFFFFFFF)
val TextBlack = Color(0xFF1C1C1E)
val LightGrey = Color(0xFFF5F5F5)        // For card backgrounds in light mode
val DarkGrey = Color(0xFF2C2C2E)         // For card backgrounds in dark mode

// Semantic Colors
val ErrorRed = Color(0xFFB00020)

// Theme-specific aliases (as used in Theme.kt)
val PrimaryBlue = DeepPurple
val AccentGold = GoldAccent
val PrimaryBlueDark = LightPurple
val AccentGoldDark = SoftGold
