<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Material 3 XML teması bulunamıyorsa AppCompat'e düşüyoruz. -->
    <!-- Compose, kendi temasını (PatiDostTheme) kullandığı için bu sadece Splash/Window için -->
    <style name="Theme.PatiDost" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>